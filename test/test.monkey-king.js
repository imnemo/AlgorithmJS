if(process.argv){	var algo = require('./../lib/monkeyKing'),	expect = require('chai').expect;}describe('monkeys vote for their king', function(){	var mk = algo.monkeyKing;	it('should throw an error if n <= 0 or m != 0', function(){		expect(function(){mk(-1, 1);}).to.throw(Error);	});	it('1 monkey and ignore the first, 1 will be the result', function(){		expect(mk(1, 1)).to.eql(1);	});	it('10 monkeys and ignore the third, 4 will be the result', function(){		expect(mk(10, 3)).to.equal(4);	});	it('10 monkeys and ignore the third but count from the reverse direction, 8 will be the result', function(){		expect(mk(10, -3)).to.equal(8);	});});